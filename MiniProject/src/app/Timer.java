package app;

public class Timer extends Thread {
/*
	static final int TIMELIMIT = 60; // 제한 시간
	
	int time;
	
	@Override
	public void run() {
		time = TIMELIMIT;
		while (time > 0) {
			try {
				Thread.sleep(1000);
				time--;
				sendMsg("T"+time);
			} catch (InterruptedException e) {
				e.printStackTrace();
			}
		}
		// 시간 초과
	}
*/
}
